---
phase: 04-package
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - template/main.typ
  - template/references.bib
autonomous: true
requirements:
  - PACK-02
  - PACK-03
  - PACK-05
  - PACK-06

must_haves:
  truths:
    - "template/main.typ compiles to a correctly-formatted MISQ submission PDF"
    - "The example document demonstrates all three heading levels with correct numbering"
    - "The example document includes both parenthetical and prose-form citation patterns"
    - "The example document includes at least one figure and at least one table with captions"
    - "The example document includes an appendix section with page break"
    - "references.bib contains sample entries covering multiple BibTeX entry types"
  artifacts:
    - path: "template/main.typ"
      provides: "Complete showcase document for MISQ template"
      min_lines: 80
    - path: "template/references.bib"
      provides: "Sample bibliography entries"
      min_lines: 20
  key_links:
    - from: "template/main.typ"
      to: "../misq.typ"
      via: "import statement"
      pattern: '#import "../misq.typ"'
    - from: "template/main.typ"
      to: "references.bib"
      via: "bibliography call"
      pattern: '#bibliography("references.bib"'
---

<objective>
Expand template/main.typ from a dev test document into a production-quality MISQ example document, and enrich references.bib with additional entry types.

Purpose: PACK-02 requires a working example demonstrating all features. PACK-03/05/06 require sample citations, figures, and tables. The current template works but reads as a test document — it needs to look like a real MISQ submission that authors can copy and adapt.
Output: A polished template/main.typ and expanded template/references.bib.
</objective>

<execution_context>
@/Users/ryanschuetzler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanschuetzler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-package/04-RESEARCH.md

@template/main.typ
@template/references.bib
@misq.typ
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand references.bib with additional entry types</name>
  <files>template/references.bib</files>
  <action>
Keep the existing 3 journal article entries (orlikowski1992duality, brown2023fault, gupta2018economic). Add 1-2 additional entries to demonstrate different BibTeX types used in IS research:

1. Add one @book entry — e.g., a well-known IS textbook or research methods book. Use a real, citable book relevant to IS research.

2. Optionally add one @inproceedings entry — e.g., an ICIS or AMCIS conference paper. Only if it adds meaningful demonstration value.

Keep total entries at 4-5 maximum. The goal is demonstrating different citation formats, not building a comprehensive bibliography.

Ensure all entries have complete fields (author, title, year, publisher/journal, pages where applicable).
  </action>
  <verify>
    <automated>typst compile --root /Users/ryanschuetzler/code/typst-misq /Users/ryanschuetzler/code/typst-misq/template/main.typ</automated>
  </verify>
  <done>references.bib contains 4-5 entries covering at least journal article and book entry types. All entries compile successfully in the bibliography.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite template/main.typ as production-quality example</name>
  <files>template/main.typ</files>
  <action>
Rewrite template/main.typ from a dev test document into a polished MISQ template example that authors can copy and adapt. Keep the same import pattern (`#import "../misq.typ": misq`) and `#show: misq.with(...)` structure.

Required content changes:

1. **Title**: Change from "MISQ Template Test Document" to something that reads as a plausible MISQ paper title (e.g., related to IS research methodology or technology adoption).

2. **Abstract**: Rewrite to read like a plausible MISQ abstract (~150-200 words). Keep it generic enough to serve as a template but professional in tone.

3. **Keywords**: Keep 4-5 IS-relevant keywords.

4. **Section structure** — maintain all three heading levels:
   - = Introduction (L1)
   - = Literature Review (L1) with == subsections (L2) and === sub-subsections (L3)
   - = Methodology (L1) with figure and table
   - = Discussion (L1) with citations
   - References (via #bibliography call)
   - Appendix

5. **Citation patterns** — add ALL required patterns:
   - Parenthetical: `@key` (already exists)
   - Prose form: `#cite(<key>, form: "prose")` (MISSING — must add)
   - Multiple citations in one reference (already exists with `@brown2023fault @gupta2018economic`)
   - Add at least one citation using the new book/conference entry from Task 1

6. **Figure**: Keep the existing figure (rect placeholder) but add a `<fig:example>` label AND a cross-reference (`@fig:example`) in the body text.

7. **Table**: Keep the existing table but add a `<tab:stats>` label AND a cross-reference (`@tab:stats`) in the body text.

8. **Appendix**: Keep the existing appendix pattern (manual #align heading). Add a comment in the template explaining why `= APPENDIX` is not used (auto-numbering would produce "5 APPENDIX").

9. **Body text**: Replace test-oriented language ("This is the introduction section of the test document", "This tests...") with professional-sounding placeholder text that reads as a realistic IS research paper. Keep text concise — this is a template, not a real paper. Each section needs 1-2 paragraphs maximum.

10. **Template comments at top**: Update the file header comment to describe this as an example document for the MISQ template package, not a "test document". Include a brief comment explaining that authors should replace the content with their own manuscript.

Preserve:
- The `#import "../misq.typ": misq` relative import (Phase 5 changes this)
- The `paragraph-style: "indent"` parameter
- The `#pagebreak()` before bibliography and before appendix
- The `#bibliography("references.bib", title: "References")` call pattern
  </action>
  <verify>
    <automated>typst compile --root /Users/ryanschuetzler/code/typst-misq /Users/ryanschuetzler/code/typst-misq/template/main.typ</automated>
  </verify>
  <done>template/main.typ compiles successfully and demonstrates: all 3 heading levels, parenthetical and prose-form citations, figure with label and cross-reference, table with label and cross-reference, appendix with page break, professional-sounding content.</done>
</task>

</tasks>

<verification>
1. `typst compile --root . template/main.typ` succeeds with no errors
2. PDF output shows all three heading levels correctly numbered
3. PDF shows both parenthetical and prose-form citations
4. PDF shows figure and table with captions
5. PDF shows appendix on its own page with centered bold heading
6. references.bib has entries of multiple types (article + book minimum)
</verification>

<success_criteria>
- template/main.typ reads as a professional MISQ template example, not a test document
- All citation patterns demonstrated (parenthetical, prose, multi-cite)
- Figure and table have labels and are cross-referenced in text
- Appendix pattern is clearly documented with comments
- `typst compile --root . template/main.typ` produces a correctly-formatted PDF
</success_criteria>

<output>
After completion, create `.planning/phases/04-package/04-02-SUMMARY.md`
</output>
