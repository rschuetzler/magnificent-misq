---
phase: 04-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typst.toml
  - misq.typ
autonomous: true
requirements:
  - PACK-01
  - PACK-04
  - PACK-07

must_haves:
  truths:
    - "typst.toml exists at project root with correct package and template metadata"
    - "misq.typ has inline comments explaining every non-obvious formatting decision"
    - "Comment gaps identified in research (bibliography block rule, figure scoping, all:true indent) are filled"
  artifacts:
    - path: "typst.toml"
      provides: "Package manifest for Typst Universe submission"
      contains: "[package]"
    - path: "misq.typ"
      provides: "Template definition with comprehensive inline comments"
      contains: "misq.typ"
  key_links:
    - from: "typst.toml"
      to: "misq.typ"
      via: "entrypoint field"
      pattern: 'entrypoint = "misq.typ"'
    - from: "typst.toml"
      to: "template/main.typ"
      via: "[template] section"
      pattern: 'path = "template"'
---

<objective>
Create the typst.toml package manifest and audit/enhance inline comments in misq.typ.

Purpose: PACK-04 requires a valid typst.toml for Typst Universe submission. PACK-01 and PACK-07 require misq.typ to be well-documented with inline comments explaining formatting decisions.
Output: typst.toml at project root; misq.typ with comprehensive comments covering all formatting rules.
</objective>

<execution_context>
@/Users/ryanschuetzler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanschuetzler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-package/04-RESEARCH.md

@misq.typ
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typst.toml package manifest</name>
  <files>typst.toml</files>
  <action>
Create typst.toml at project root following the ambivalent-amcis pattern from research.

Required fields in [package] section:
- name = "misq"
- version = "0.1.0"
- entrypoint = "misq.typ" (NOT template/main.typ — this is the package library, not the template)
- authors = ["Ryan Schuetzler <ryan.schuetzler@byu.edu>"]
- license = "MIT"
- description = "A Typst template for MIS Quarterly manuscript submissions"
- keywords = ["MISQ", "MIS Quarterly", "academic", "management information systems", "APA"]
- categories = ["paper"]
- disciplines = ["computer-science"] (nearest valid Typst taxonomy match for IS research)
- exclude = ["template/main.pdf"]

Required fields in [template] section:
- path = "template"
- entrypoint = "main.typ" (relative to template path)
- thumbnail = "thumbnail.png" (file does not exist yet — Phase 5 generates it; declaring path here is safe per research)

Do NOT add repository or homepage fields unless they exist in the ambivalent-amcis pattern.
  </action>
  <verify>
    <automated>cat /Users/ryanschuetzler/code/typst-misq/typst.toml && typst compile --root /Users/ryanschuetzler/code/typst-misq /Users/ryanschuetzler/code/typst-misq/template/main.typ</automated>
  </verify>
  <done>typst.toml exists at project root with [package] and [template] sections. typst compile still succeeds (typst.toml does not break compilation).</done>
</task>

<task type="auto">
  <name>Task 2: Audit and enhance misq.typ inline comments</name>
  <files>misq.typ</files>
  <action>
Audit every set/show rule in misq.typ and fill comment gaps identified in research. Do NOT change any code — only add or improve comments.

Specific gaps to fill (from 04-RESEARCH.md "Missing comments" section):

1. Bibliography block show rule (around line 50-62): Add comment explaining WHY hanging-indent requires the block-level show rule approach. Reference the CSL hanging-indent="false" requirement and Typst GitHub issue #2639 workaround. Explain that CSL hanging-indent="true" blocks Typst's par-level hanging indent override, so the CSL ships with hanging-indent="false" and the show rule applies par(hanging-indent: 0.5in) at the block level instead.

2. Figure single-spacing rule (around line 66): Expand comment to explain the `show figure: set par()` scoping mechanism — this is a set rule attached to a show rule, meaning the par settings only apply within figure content, not globally.

3. First-line-indent `all: true` (around line 40): Add comment explaining that `all: true` is required so that paragraphs immediately following headings also get the first-line indent. Without `all: true`, Typst skips indent on the first paragraph after a heading (which matches some style guides but not MISQ's requirement).

Keep the existing comment style convention:
```
// --- Section name (REQUIREMENT-IDs) ---
// What the rule does: plain English
// Why this value/approach: calibration note, constraint reference
```

Do NOT remove or modify any existing comments that are already correct. Only ADD missing explanations and EXPAND partial ones.
  </action>
  <verify>
    <automated>typst compile --root /Users/ryanschuetzler/code/typst-misq /Users/ryanschuetzler/code/typst-misq/template/main.typ</automated>
  </verify>
  <done>All three identified comment gaps are filled. misq.typ compiles identically (no code changes, only comments). Every set/show rule in the file has an inline comment explaining the formatting decision.</done>
</task>

</tasks>

<verification>
1. typst.toml exists and contains [package] with name="misq", entrypoint="misq.typ", and [template] with path="template"
2. misq.typ compiles successfully with no code changes
3. All three comment gaps from research are addressed
</verification>

<success_criteria>
- typst.toml is a valid TOML file with all required fields for Typst Universe submission
- misq.typ has inline comments on every non-obvious formatting rule
- `typst compile --root . template/main.typ` still succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-package/04-01-SUMMARY.md`
</output>
