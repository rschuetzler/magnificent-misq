---
phase: 04-package
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [misq.typ]
autonomous: true
requirements: [PACK-01, PACK-02, PACK-03, PACK-04, PACK-05, PACK-06, PACK-07]
gap_closure: true

must_haves:
  truths:
    - "misq.typ bibliography comment accurately describes that apa.csl has hanging-indent='true' (native CSL support)"
    - "misq.typ bibliography comment does NOT reference par(hanging-indent: 0.5in) or apa.csl hanging-indent='false'"
    - "misq.typ bibliography comment explains the show rule handles only single-spacing and REFERENCES heading formatting"
  artifacts:
    - path: "misq.typ"
      provides: "Accurate inline comments for bibliography formatting"
      contains: "hanging-indent.*true"
  key_links: []
---

<objective>
Fix the factually incorrect bibliography comment in misq.typ (lines 53-63).

Purpose: The verification report found that the bibliography section comment describes a superseded architecture — it claims apa.csl has hanging-indent="false" and that the show rule applies par(hanging-indent: 0.5in), neither of which is true. The actual apa.csl has hanging-indent="true" (unmodified upstream), and the show rule only handles single-spacing and the REFERENCES heading. This gap prevents PACK-07 from being fully satisfied.

Output: Corrected comment block in misq.typ that accurately describes how bibliography formatting works.
</objective>

<execution_context>
@/Users/ryanschuetzler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanschuetzler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-package/04-VERIFICATION.md
@misq.typ
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace incorrect bibliography comment with accurate description</name>
  <files>misq.typ</files>
  <action>
Replace lines 53-63 in misq.typ (the bibliography formatting comment block) with an accurate description. The new comment must:

1. State that the bundled apa.csl is an unmodified upstream APA 7th edition CSL file
2. State that apa.csl has hanging-indent="true", which means the CSL itself delivers the 0.5-inch hanging indent natively — no Typst-level hanging-indent code is needed
3. Explain that the show bibliography rule handles ONLY two concerns: single-spacing (par leading/spacing at 0.65em) and the REFERENCES heading formatting (STRC-03: centered, bold, uppercase)
4. Keep the existing explanation about why a single combined show rule is needed (multiple transformational show rules overwrite each other)
5. Remove ALL references to: apa.csl hanging-indent="false", par(hanging-indent: 0.5in), Typst GitHub issue #2639 workaround, and "block-level" hanging indent application

The comment should follow the established pattern in misq.typ:
```
// --- Section name (REQUIREMENT-IDs) ---
// What the rule does: plain English
// Why this value/approach: rationale
```

Preserve all code below the comment (lines 64-76) exactly as-is — only the comment text changes.
  </action>
  <verify>
    <automated>cd /Users/ryanschuetzler/code/typst-misq && grep -c 'hanging-indent="false"' misq.typ | grep -q '^0$' && grep -c 'par(hanging-indent' misq.typ | grep -q '^0$' && grep -q 'hanging-indent.*true' misq.typ && typst compile --root . template/main.typ && echo "PASS"</automated>
  </verify>
  <done>The bibliography comment in misq.typ accurately states that apa.csl has hanging-indent="true" (native CSL support), explains the show rule handles only single-spacing and REFERENCES heading formatting, and contains no references to the removed workaround architecture. Template still compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `grep 'hanging-indent="false"' misq.typ` returns no matches (removed false claim)
2. `grep 'par(hanging-indent' misq.typ` returns no matches (removed false code reference)
3. `grep 'hanging-indent.*true' misq.typ` returns a match (correct description present)
4. `typst compile --root . template/main.typ` exits 0 (no compilation regression)
5. Read the comment block and confirm it accurately describes the current code behavior
</verification>

<success_criteria>
- The bibliography comment in misq.typ is factually correct, describing the actual architecture
- No references to the superseded workaround (hanging-indent="false", par(hanging-indent:), issue #2639 workaround)
- PACK-07 is fully satisfied — all inline comments in misq.typ are accurate
- Template compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-package/04-03-SUMMARY.md`
</output>
