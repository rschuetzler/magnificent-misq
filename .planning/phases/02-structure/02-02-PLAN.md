---
phase: 02-structure
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - template/main.typ
autonomous: false
requirements:
  - STRC-03
  - STRC-04

must_haves:
  truths:
    - "template/main.typ compiles successfully with updated misq() call (no bib param)"
    - "template/main.typ demonstrates all three heading levels with visible numbering"
    - "template/main.typ includes a manually formatted REFERENCES section"
    - "template/main.typ includes a manually formatted APPENDIX section"
    - "PDF shows correct visual output for all Phase 2 requirements"
  artifacts:
    - path: "template/main.typ"
      provides: "Test document exercising all Phase 2 features"
      contains: "== "
  key_links:
    - from: "template/main.typ"
      to: "misq.typ"
      via: "#show: misq.with(...) call with paragraph-style param, no bib param"
      pattern: "misq.with"
---

<objective>
Update template/main.typ to exercise all Phase 2 features: remove bib parameter, add level 2 and level 3 heading examples, add manual REFERENCES and APPENDIX sections, and test paragraph-style parameter. Then visually verify the compiled PDF.

Purpose: Proves all structural features work correctly and demonstrates STRC-03/STRC-04 (author-managed REFERENCES/APPENDIX formatting).
Output: Updated template/main.typ that compiles and demonstrates all Phase 2 requirements.
</objective>

<execution_context>
@/Users/ryanschuetzler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanschuetzler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structure/02-CONTEXT.md
@.planning/phases/02-structure/02-RESEARCH.md
@.planning/phases/02-structure/02-01-SUMMARY.md

<interfaces>
<!-- Updated misq() signature from Plan 01: -->
From misq.typ (after Plan 01):
```typst
#let misq(
  title: [Untitled],
  abstract: none,
  keywords: (),
  paragraph-style: "indent",  // "indent" or "block"
  body
) = { ... }
```

Heading levels available:
- `= Level 1` renders centered, uppercase, bold, numbered (1, 2, 3)
- `== Level 2` renders centered, bold, numbered (1.1, 1.2)
- `=== Level 3` renders left-aligned, bold, numbered (1.1.1, 1.1.2)

REFERENCES/APPENDIX: Author handles manually (no template automation).
Bibliography: Author calls `#bibliography(...)` explicitly in document body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update template/main.typ for Phase 2 features</name>
  <files>template/main.typ</files>
  <action>
Rewrite template/main.typ to exercise all Phase 2 features:

1. **Update misq.with() call:**
   - Remove `bib: "template/references.bib"` parameter
   - Add `paragraph-style: "indent"` parameter (or omit since it is the default — include explicitly for documentation clarity)
   - Keep title, abstract, keywords as-is

2. **Add heading level examples:**
   - Keep existing `= Introduction` (level 1 — will now render as "1 INTRODUCTION" centered uppercase bold)
   - Add `== Subsection Example` under a body section to demonstrate level 2 (centered, bold, numbered)
   - Add `=== Sub-subsection Example` to demonstrate level 3 (left-aligned, bold, numbered)
   - Structure the document with at least 2 level-1 headings so numbering progression is visible (e.g., 1 INTRODUCTION, 2 LITERATURE REVIEW with subsections)

3. **Document structure — use realistic academic section names:**
   ```
   = Introduction          (renders as: 1 INTRODUCTION)
   [body text]

   = Literature Review     (renders as: 2 LITERATURE REVIEW)
   [body text]
   == Theoretical Background   (renders as: 2.1 Theoretical Background)
   [body text]
   === Key Constructs          (renders as: 2.1.1 Key Constructs)
   [body text]

   = Methodology           (renders as: 3 METHODOLOGY)
   [body text]

   = Discussion            (renders as: 4 DISCUSSION)
   [body text with citations]
   ```

4. **Add explicit bibliography call:**
   - After the last body section, add:
     ```typst
     #bibliography("references.bib", style: "apa")
     ```
   - Note the path is relative to template/main.typ, so just "references.bib" (the file is in the same directory)

5. **Add manual REFERENCES heading (STRC-03):**
   - Actually, the `#bibliography()` call in Typst auto-generates the "References" heading. The user decision says "no auto-styling via show rules" for REFERENCES — meaning we don't add special show rules to transform it. The bibliography's own heading will appear. This is fine — STRC-03 is satisfied by the bibliography rendering with its heading.
   - If the bibliography heading does not render as centered uppercase "REFERENCES", we accept the bibliography's default heading style since the user decided against auto-styling show rules.

6. **Add manual APPENDIX section (STRC-04):**
   - After the bibliography, add:
     ```typst
     #pagebreak()
     #align(center, text(weight: "bold", size: 12pt)[APPENDIX])

     This appendix contains supplementary materials...
     ```
   - This demonstrates the author-managed approach per user decision

7. **Keep existing figure and table examples** — they test the single-spacing show rules from Phase 1.

8. **Update citations** — keep the `@brown2023fault` and `@gupta2018economic` citations in the Discussion section.
  </action>
  <verify>
    <automated>typst compile template/main.typ template/main.pdf 2>&1</automated>
  </verify>
  <done>template/main.typ compiles. Document shows: page 1 with bold centered title, centered "Abstract" label, keywords, then page break. Page 2+ shows numbered headings at all three levels. Bibliography appears with references. Appendix on its own page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 2 document structure</name>
  <files>template/main.pdf</files>
  <action>
Compile the template and open the PDF for human visual verification of all Phase 2 requirements.

Run: `typst compile template/main.typ template/main.pdf && open template/main.pdf`

Verify all of the following:
1. **Page 1:** Bold centered title (original casing, NOT uppercase), centered bold "Abstract" label, left-aligned bold "Keywords:" line
2. **Page 2:** Introduction starts at top of page 2 (no blank page between)
3. **Headings:** Level 1 centered, UPPERCASE, bold, numbered (1, 2, 3...); level 2 centered, bold, numbered (1.1, 1.2...); level 3 left-aligned, bold, numbered (1.1.1...)
4. **Heading numbers:** No trailing periods on numbers
5. **Paragraph style:** First-line indent on body paragraphs (except first after heading)
6. **Footer:** Page numbers centered at bottom of every page
7. **Spacing:** Heading spacing looks reasonable (uniform before/after all levels)
8. **Bibliography:** References appear with single-spacing
9. **Appendix:** APPENDIX appears on its own page with centered bold heading
10. Optionally compare against Word template (MISQ-Manuscript-Template-Word.docx) for title page spacing
  </action>
  <verify>
    <automated>typst compile template/main.typ template/main.pdf 2>&1</automated>
  </verify>
  <done>User confirms PDF output matches all Phase 2 requirements: correct title page, heading styles with numbering, page flow, paragraph indentation, page numbers, bibliography, and appendix.</done>
</task>

</tasks>

<verification>
- `typst compile template/main.typ template/main.pdf` succeeds with no errors
- PDF visual inspection confirms all Phase 2 requirements
</verification>

<success_criteria>
- template/main.typ compiles without errors
- All three heading levels display with correct styling and numbering
- Title page layout matches MISQ requirements
- Page flow is correct (Introduction on page 2)
- REFERENCES section visible via bibliography
- APPENDIX section on its own page
- User approves visual output at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-structure/02-02-SUMMARY.md`
</output>
