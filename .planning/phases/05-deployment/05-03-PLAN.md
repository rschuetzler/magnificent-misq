---
phase: 05-deployment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: false
gap_closure: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04

must_haves:
  truths:
    - "The import line in README.md matches the package name in typst.toml exactly"
    - "A user following the README import line will find the package on Typst Universe"
  artifacts:
    - path: "README.md"
      provides: "Package usage documentation with correct import syntax"
      contains: "The correct @preview package name"
  key_links:
    - from: "README.md"
      to: "typst.toml"
      via: "Package name consistency"
      pattern: "@preview/(magnificent-misq|misq)"
---

<objective>
Fix package name mismatch between README.md and typst.toml.

Purpose: README.md shows `@preview/misq:0.1.0` as the import line, but typst.toml declares `name = "magnificent-misq"`. The Makefile uses `PKG_NAME := magnificent-misq`, so the package will be submitted to Typst Universe under `packages/preview/magnificent-misq/0.1.0/`. A user following the README import line gets "package not found" because no package named `misq` exists on Typst Universe.

The fix requires a naming decision — the public Typst Universe package name is permanent once published.

Output: README.md with an import line that matches the registered package name.
</objective>

<execution_context>
@/Users/ryan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deployment/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Decision: Choose the Typst Universe package name</name>
  <action>Present the naming decision to the user and wait for a choice before proceeding to Task 1.</action>
  <verify>User has selected option-a or option-b.</verify>
  <done>User decision recorded in session context.</done>
  <decision>
The package has a naming inconsistency that must be resolved before publishing:

- typst.toml:  name = "magnificent-misq"  (what Typst Universe registers)
- Makefile:    PKG_NAME := magnificent-misq  (what gets submitted to fork)
- README.md:   #import "@preview/misq:0.1.0": misq  (what users are told to type)

typst.toml and Makefile are consistent with each other. README.md is inconsistent with them.
Once published to Typst Universe, the package name is permanent.

OPTION A — Keep "magnificent-misq" (fix README only):
  Change README.md line 12 to: #import "@preview/magnificent-misq:0.1.0": misq
  Pros: typst.toml and Makefile already correct; one-line fix; matches GitHub repo name
  Cons: Longer import string for users

OPTION B — Rename to "misq" (fix typst.toml and Makefile):
  Change typst.toml name field to: name = "misq"
  Change Makefile to: PKG_NAME := misq
  README.md is already correct as-is
  Pros: Shorter, cleaner import; README is already written this way
  Cons: Must verify "misq" is not already taken on Typst Universe before choosing

In either option: the README title ("# misq") and Typst function name ("misq") stay the same.
  </decision>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Apply chosen package name consistently</name>
  <files>README.md, typst.toml, Makefile</files>
  <action>
Based on the decision checkpoint, apply whichever fix was chosen:

IF option-a (keep "magnificent-misq"):
- Edit README.md line 12: change `#import "@preview/misq:0.1.0": misq` to `#import "@preview/magnificent-misq:0.1.0": misq`
- No changes to typst.toml or Makefile — they are already correct

IF option-b (rename to "misq"):
- Edit typst.toml: change `name = "magnificent-misq"` to `name = "misq"`
- Edit Makefile: change `PKG_NAME    := magnificent-misq` to `PKG_NAME    := misq`
- README.md line 12 is already correct — no change needed

After applying the fix, verify consistency: the package name must appear identically in all three files:
- typst.toml `name` field
- Makefile `PKG_NAME` variable
- README.md import line `@preview/{name}:`

Note: The function import alias (`misq` in `: misq`) stays the same regardless of option — it refers to the Typst function name, not the package name.
  </action>
  <verify>
    <automated>grep -n "magnificent-misq\|@preview/misq\|PKG_NAME\|^name" /Users/ryan/code/magnificent-misq/typst.toml /Users/ryan/code/magnificent-misq/Makefile /Users/ryan/code/magnificent-misq/README.md</automated>
  </verify>
  <done>All three files reference the same package name. The import line in README.md matches what Typst Universe will register the package under.</done>
</task>

</tasks>

<verification>
After the fix, confirm all three sources agree on the package name:

```bash
grep '^name' /Users/ryan/code/magnificent-misq/typst.toml
grep 'PKG_NAME' /Users/ryan/code/magnificent-misq/Makefile | head -1
grep '@preview/' /Users/ryan/code/magnificent-misq/README.md
```

The `@preview/` prefix in README.md must be followed by exactly the value in typst.toml `name`.
</verification>

<success_criteria>
- README.md import line matches the typst.toml package name
- typst.toml, Makefile PKG_NAME, and README.md all use the same package name
- A user following the README import line will reach the published package on Typst Universe
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment/05-03-SUMMARY.md` following the standard summary template.
</output>
