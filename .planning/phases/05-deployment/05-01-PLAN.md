---
phase: 05-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - LICENSE
autonomous: true
requirements:
  - DEPLOY-03
must_haves:
  truths:
    - "README.md exists at project root with usage instructions for the MISQ template"
    - "LICENSE file exists at project root with MIT license text"
  artifacts:
    - path: "README.md"
      provides: "Package documentation for Typst Universe"
      contains: "misq"
    - path: "LICENSE"
      provides: "MIT license required by typst.toml declaration"
      contains: "MIT License"
  key_links:
    - from: "README.md"
      to: "typst.toml"
      via: "Package name and import syntax must match"
      pattern: "misq"
---

<objective>
Create README.md and LICENSE files required for Typst Universe submission.

Purpose: Typst Universe CI rejects packages without README.md. The publish script's include list references both README.md and LICENSE. These must exist before `make publish` can run.
Output: README.md with usage docs, LICENSE with MIT text
</objective>

<execution_context>
@/Users/ryanschuetzler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanschuetzler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@typst.toml
@template/main.typ
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with usage documentation</name>
  <files>README.md</files>
  <action>
Create README.md at the project root. Content should include:

1. **Title**: "misq" — A Typst template for MIS Quarterly manuscript submissions
2. **Brief description**: One paragraph explaining the template reproduces MISQ formatting requirements in Typst
3. **Usage section**: Show the Typst Universe import syntax:
   ```typst
   #import "@preview/misq:0.1.0": misq
   ```
   Then show the basic document setup calling `misq()` with parameters (title, abstract, keywords, paragraph-style). Reference template/main.typ as the full example.
4. **Parameters section**: Document the `misq()` function parameters:
   - `title` (content): Manuscript title
   - `abstract` (content): Abstract text
   - `keywords` (array of strings): Keyword list
   - `paragraph-style` (string, default "indent"): "indent" for first-line indent or "block" for block paragraphs
5. **Features section**: Bullet list — three heading levels, APA 7th citations via bundled CSL, title page with abstract/keywords, double-spaced body, correct margins
6. **Bibliography section**: Brief note that authors call `#bibliography("references.bib", title: "References")` and the template auto-applies APA 7th formatting
7. **License**: "MIT" with link to LICENSE file

Keep it concise — this is a Typst Universe package README, not full documentation. No emojis. Use standard markdown.

Do NOT include a version number in the title or description that would need updating — just use the package name.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "misq" README.md && grep -q "@preview/misq" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md exists at project root with import syntax, parameter docs, and feature list</done>
</task>

<task type="auto">
  <name>Task 2: Create MIT LICENSE file</name>
  <files>LICENSE</files>
  <action>
Create LICENSE file at the project root with the standard MIT License text.

- Copyright line: "Copyright (c) 2026 Ryan Schuetzler" (matching the author in typst.toml)
- Use the standard MIT License body text (permission notice, warranty disclaimer)
- No modifications to the standard text
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q "MIT License" LICENSE && grep -q "Ryan Schuetzler" LICENSE && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>LICENSE file exists with MIT license text and correct copyright holder</done>
</task>

</tasks>

<verification>
Both files exist at project root and contain expected content. `typst compile template/main.typ` still succeeds (no regression).
</verification>

<success_criteria>
- README.md exists with Typst Universe import syntax and parameter documentation
- LICENSE exists with MIT license text
- Both files ready for inclusion in the publish script's file list
</success_criteria>

<output>
After completion, create `.planning/phases/05-deployment/05-01-SUMMARY.md`
</output>
